ge_df.expect_column_values_to_not_be_null("id") = essa regra exigi que o dado "ID" não pode vir vazio

ge_df.expect_column_values_to_be_unique("id") = essa regra exigi que o dado "ID" seja único.

